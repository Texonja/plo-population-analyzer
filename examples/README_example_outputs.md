# Example Output Notes

Do not commit real raw hand histories. For public GitHub usage, commit only sanitized sample reports/screenshots or synthetic hand histories.

Recommended public screenshots later:

1. `pool_dashboard.html` top cards + key leak chart.
2. `charts/spot_pottype_heatmap.png`.
3. `charts/response_mix.png`.
4. `fourbet_dashboard.html` AAxx by position chart.
